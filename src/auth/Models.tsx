
export interface CredentialsModel {
    email: string;
    password: string;
}

export interface RegisterModel {
    fname: string
    lname: string
    email: string
    password: string
    confirmPassword: string
    dob: Date
    contact: string
    img_address: string
}